# Database Configuration (tương tự Laravel .env)
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=nestjs_db

# App Configuration
NODE_ENV=development
PORT=3000

# JWT Settings (sẽ dùng sau)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h
